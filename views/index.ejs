<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Mensagens</title>
        <link rel="stylesheet" href="style.css" />
    </head>
    <body>
        <h1>Mensagens Portfolio</h1>
        <% if (data.length > 0) { %> <% for( let i = 0; i < data.length; i++ ) {
        %>
        <div class="mensagem">
            <h2>Nome: <%= data[i].nome %></h2>
            <p>Email: <%= data[i].email %></p>
            <p>Assunto: <%= data[i].assunto %></p>
            <p>Mensagem: <%= data[i].mensagem %></p>
            <form action="/apagar" method="post">
                <input
                    type="hidden"
                    name="idApagada"
                    id="idApagada"
                    value="<%= data[i].id%>"
                />
                <input type="submit" value="🗑️" />
            </form>
        </div>
        <% } %> <% } %>
    </body>
</html>
